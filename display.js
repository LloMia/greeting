function greet(name, language) {
  if(language === "english"){
    return "Hello!, " + name
  }
   if(language === "setswana"){
    return "Dumela!, " + name
  }
   if(language === "french"){
    return "Bonjour!, " + name
  }
}
